import { ArrowLeft, Bot, CheckCircle2, Clock3, Package, ShieldCheck } from "lucide-react";
import Link from "next/link";
import { merchants } from "@/lib/demo-data";

export default async function MerchantDemoPage({ params }: { params: Promise<{ merchantId: string }> }) {
  const { merchantId } = await params;
  const merchant = merchants.find((item) => item.id === merchantId) ?? merchants[2];
  return <main className="min-h-screen bg-slate-50 px-5 py-8 text-slate-800"><div className="mx-auto max-w-3xl"><Link href="/" className="inline-flex items-center gap-1.5 text-sm font-bold text-slate-500 hover:text-slate-900"><ArrowLeft size={16} /> Back to buyer demo</Link><div className="mt-7 overflow-hidden rounded-[26px] border border-slate-200 bg-white shadow-soft"><header className="flex items-center justify-between bg-slate-900 px-6 py-5 text-white"><div className="flex items-center gap-3"><span className={`grid h-10 w-10 place-items-center rounded-xl ${merchant.color}`}>{merchant.initials}</span><div><p className="font-extrabold">{merchant.name}</p><p className="text-xs text-slate-300">Merchant agent inbox</p></div></div><span className="rounded-full bg-emerald-400/15 px-3 py-1 text-xs font-bold text-emerald-300">Category-agnostic quotation request</span></header><div className="p-6"><div className="flex gap-3 rounded-2xl bg-blue-50 p-4"><Bot className="shrink-0 text-blue-600" size={19} /><div><p className="text-xs font-bold uppercase tracking-wider text-blue-600">New AI buyer request</p><p className="mt-1.5 text-sm font-semibold leading-6 text-slate-700">A buyer has submitted a product or service quotation request with a delivery city, budget cap, and commercially comparable requirements.</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-3">{[[Package, "Product match", "Buyer specifications"], [Clock3, "Delivery", "Buyer deadline"], [ShieldCheck, "Buyer budget", "Approved cap"]].map(([Icon, label, value]) => { const Visual = Icon as typeof Package; return <div key={label as string} className="rounded-xl border border-slate-100 p-3"><Visual className="text-blue-600" size={17} /><p className="mt-2 text-[10px] font-bold uppercase tracking-wider text-slate-400">{label as string}</p><p className="mt-1 text-xs font-bold text-slate-700">{value as string}</p></div> })}</div><div className="mt-6 space-y-4 border-t border-slate-100 pt-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Conversation</p><div className="max-w-[85%] rounded-2xl rounded-tl-md bg-slate-100 px-4 py-3 text-sm leading-6 text-slate-600">Buyer Agent: Please confirm product match, tax, delivery, warranty or support, and final payable amount.</div><div className="ml-auto max-w-[85%] rounded-2xl rounded-tr-md bg-blue-600 px-4 py-3 text-sm leading-6 text-white">{merchant.name}: I can confirm the product and final commercial terms for this quotation request.</div><div className="max-w-[85%] rounded-2xl rounded-tl-md bg-slate-100 px-4 py-3 text-sm leading-6 text-slate-600">Buyer Agent: Another qualifying offer has a lower effective cost. Can you improve your final term without sharing competitor details?</div></div><div className="mt-6 flex items-center gap-2 rounded-xl bg-emerald-50 px-3 py-2.5 text-xs font-bold text-emerald-700"><CheckCircle2 size={15} /> The buyer agent never exposes a competitor’s identifiable offer.</div></div></div></div></main>;
}
